# 싹싹(Ssak-ssak) 디자인 시스템 v4 — "그린 차분"

> **정본.** 2026-09-11 리브랜딩 반영. 기존 v3(코랄/토스 스타일)를 대체한다.
> 시각 시안: https://claude.ai/code/artifact/931a1bc7-5efc-484d-b55a-bbf24aff614f (전 화면 적용전/후)
> 리브랜딩 기준 배포본: V1.9.103 (base 47714ca)

---

## 0. 브랜드 정체성

**싹싹 = 싹싹하다(싹싹한 태도) + 싹싹 문질러 닦다 + 새싹.**
사장님이 매일 쓰는 **믿음직한 운영 도구**. 화면은 조용하고(무채색), 중요한 것 하나만 그린으로 말한다.

- 무드: 정갈함 · 신선함 · 프로의 도구. 귀여움/마스코트 없음.
- 한 줄 원칙: **"모두가 외치면 아무도 안 들린다"** — 그린은 화면당 한 번만 크게 외친다.

---

## 1. 워드마크 (락업 A — 확정)

```
싹싹  SSAK-SSAK
```

구현: `design.css .ds-lockup` (`.lk-ko` + `.lk-en`, 대형은 `.lk-lg`).

| 요소 | 규격 |
|---|---|
| 한글 `싹싹` | Pretendard 800, letter-spacing -0.03em, **그린 `--c-pr`** |
| 영문 `SSAK-SSAK` | 한글 크기의 **0.42em**, 700, letter-spacing **0.2em**, 대문자, `--c-g400` |
| 간격 | 한글-영문 사이 8px (한글 22px 기준) |

- **5탭 화면(홈·스케줄·커뮤니티·동료·MY) + 로그인 헤더 공통.**
- **고객 대면 페이지(booking/sign/quote 등)는 업체명이 브랜드 — 락업 미사용.**
- 스플래시/단독: `.lk-lg`(38px) + 태그라인 `CLEAN · CARE · CONNECT`.
- 영문 단독 사용 금지. 그린 배경 위에서는 한글 흰색 반전.

---

## 2. 컬러 토큰 (design.css `:root` — 변수명은 v3 유지, 값만 v4)

### 2-1. 무채색 (바탕)

```css
--c-bg:    #FFFFFF;   /* 앱 배경 (순백) */
--c-g100:  #F5F7F6;   /* 구획 fill, chip bg */
--c-g200:  #EBEEEC;   /* border, divider (헤어라인) */
--c-g300:  #D9DEDB;   /* disabled border */
--c-g400:  #9AA09C;   /* placeholder, 비활성 */
--c-g500:  #6A716D;   /* secondary text */
--c-g700:  #474E4A;   /* body text */
--c-g900:  #161A18;   /* title 잉크 */
```

> 뉴트럴은 **그린 미세 바이어스** 회색. 임의 회색 추가 금지.

### 2-2. 브랜드 액센트 (단 1종)

```css
--c-pr:    #4F9E7C;   /* 그린 차분 — 유일한 브랜드 액센트 */
--c-pr-d:  #3E8467;   /* hover/pressed */
--c-pr-l:  #EAF2EE;   /* 틴트 (아이콘 bg, 활성 칩) */
--c-pr-xl: #F3F8F5;   /* 옅은 틴트 (히어로 카드 bg) */
```

`--c-teal`·`--c-mint` 계열은 **참조 호환용으로 그린 계열에 흡수**됨 — 신규 사용 금지.

### 2-3. 기능색 (semantic — 브랜드색 아님, 의미 전달 소면적 전용)

```css
--c-red: #E5484D;  --c-amber: #D99A2B;  --c-blue: #3B82F6;
/* 날씨 */ --wx-sun/#E1A23C  --wx-cloud/#8B949B  --wx-rain/#4A90C2 (+-ink 짝)
/* 주말 */ --cal-sun: #B06A6A;  --cal-sat: #5B87A8;
```

- **홈 예보 카드의 날씨 씬(태양·구름·비·눈·번개·밤)은 기능색 영역 — 그린에 가두지 않는다.** 실시간 날씨가 색으로 즉시 읽히는 것이 이 카드의 존재 이유.
- 활동량 히트스케일(링·미니바): 블루 → 앰버 → **레드 #E5484D** (코랄 아님).

### 2-4. 색 위계 규칙 ★ 핵심

1. **그린 히어로(큰 숫자/강조 면)는 화면당 정확히 1곳.**
   홈 = 예보 카드(날씨 씬이 히어로 역할) · 스케줄 = 오늘 날짜 뱃지 · MY = 이달 매출.
2. **소면적 상태표시는 그린 허용:** 활성 탭 언더라인·활성 필터칩·타임바/게이지 fill·펄스 닷·FAB·복사 배지.
3. **그 외 데이터 숫자·텍스트는 잉크(`--c-g900`).** 강조는 굵기(최대 800)로. (활동량 kcal, MY 신뢰지표·건수 등)
4. **기능색은 의미가 있는 자리에만.** 장식 금지.
5. **금지:** 코랄(#FF385C 계열) 재도입, 민트/틸/인디고/퍼플 외래 액센트, 그라데이션 배경(기능 씬 제외), 다색 파스텔 아이콘 배경, 신규 이모지 아이콘.

---

## 3. 타이포그래피

Pretendard Variable (유지). 굵기 900 금지(800 최대 — 기존 900은 점진 정리). 숫자 `tabular-nums`.
문장 중간 강제 `<br>` 금지 — `keep-all` 자동 줄바꿈.

---

## 4. 그림자 · 반경

- **최소주의**: 기본 카드 = 보더 `1px --c-g200`(또는 `--sh-xs~sm`). v3의 5단계 발광 elevation·컬러 글로우·top-highlight 폐기(토큰은 참조 호환으로 은은한 값 유지).
- `--sh-pr` 계열 = 그린 소프트 그림자, FAB·주요 CTA 전용.
- 반경: 카드 16~18px, 버튼 13px(pill 허용), 칩 full.

---

## 5. 컴포넌트 요점

- **Primary 버튼**: 그린 fill + 흰 700. 파괴적 동작만 `--c-red`.
- **히어로 카드**: `--c-pr-xl` bg + 그린 16% 보더 — 화면당 1개.
- **상태 칩**: 진행/확정 = `--c-pr-l`+진그린 / 대기 = `--c-g100`+`--c-g500`.
- **필터 칩 활성 = 그린 fill** (`.ds-chip.on`·`.ds-filter-chip.on`·workforce `.fbtn.on`).
- **콘텐츠 탭**: 활성 = 잉크 텍스트 + **그린 언더라인**(`.twin-ind`·`.tab-ind`).
- **뷰 전환 세그(주/월/년)**: 뉴트럴 — 활성 = 흰 pill + 잉크/그린 텍스트 (그린 fill 안 씀).
- **하단 네비**: V1.9.101 화이트 바 + 곡선 유지, 활성 = 그린(코랄→그린 치환 완료). FAB 그린.
- **캘린더**: 오늘 = 그린 원(히어로), 일정 닷 그린, 주말 = `--cal-sun`/`--cal-sat`.

---

## 6. 접근성

잉크/흰 15.2:1 · `--c-g500` 4.9:1(AA). `--c-g400`은 본문 금지. 그린 fill 위 흰 700+. 터치 44px.
날씨 색은 색+아이콘 형태+텍스트 3중 전달(색 단독 금지).

---

## 7. v4 반영 이력 (2026-09-11 1차)

- design.css 토큰 전면 교체(변수명 유지) + `.ds-lockup` 신설
- 전 20파일 코랄/틸/민트 하드코딩 ~500곳 치환 (잔여 0 검증)
- 워드마크 락업 A: 5탭+로그인 헤더 삽입, 로그인 배경사진(코랄 자산) 폐기 → 화이트
- 색 위계: 활동량 카드 뉴트럴화(레벨 그라데이션·캐릭터 폐지), MY 숫자 잉크화, 탭 언더라인·필터칩 그린화
- 히트스케일 코랄 → 레드(기능색)
- **미완(2차)**: 이모지 아이콘 → 라인 아이콘 전환, icon-192/512·hero 이미지 그린 버전, 굵기 900 정리
